export enum ErrorLevel {
    WARN = 'warn',
    ERROR = 'error',
}

export enum RouterCategory {
    PRIVATE = 'private',
    PUBLIC = 'public',
    VIRTURE = 'virture',
}

export enum Method {
    GET = 'GET',
    POST = 'POST',
    PUT = 'PuT',
    DELETE = 'DELETE',
    PATCH = 'PATCH',
}
